// The navigation is now built once, for every page, by buildNav() in shared.js.
// This file is kept only because ~7 pages still <script src="nav.js"> it inline;
// it is intentionally a no-op so those includes don't build a second, competing
// nav. Safe to delete once those inline includes are removed.
/* nav is built by shared.js */
